% slices image of grid into 9 images of cells
function [images] = getCells(grid)
    [x, y] = size(grid);
    x = x/3;
    y = y/3;
    for i = 1:3 %row
        for j = 1:3 %col
            % row, col
            g = image(x*(i - 1):i*x, y * (j - 1) : y * j)
end