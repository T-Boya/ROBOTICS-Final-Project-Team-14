clc;
p1 = 'We have two algorithms:';
p2 = '1. Passive algorithm.';
p3 = '2. Aggressive Algorithm.';
str = [p1 newline p2 newline p3 newline];
fprintf(str);
input('Which do you want to play against? (type 1 or 2): ');
fprintf('FOOL, you have chosen algorithm: WIN AT ALL COSTS');
fprintf(newline);