%%  single move test
close all; clear all; clc
board = ['O' '_' '_'; '_' 'O' '_'; '_' '_' '_'];
[row, col] = choosemove(board, 'X');

%% game simulation
close all; clear all; clc
board = ['O' '_' '_'; '_' 'O' '_'; '_' '_' '_'];
[row, col] = choosemove(board, 'X');

for 